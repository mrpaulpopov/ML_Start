import numpy as np

def rmse(y, y_hat):
    n = len(y)
    errors = np.subtract(y, y_hat)
    return np.sqrt(1.0/n * (np.dot(errors, errors)))

    # ======================================
    # Скалярное произведение само на себя - это просто удобный способ закодить сумму квадратов
    # каждого компонента вектора.
    # ======================================